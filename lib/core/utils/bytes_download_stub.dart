import 'dart:typed_data';

Future<void> saveBytesFileImpl({
  required String filename,
  required Uint8List bytes,
  required String mimeType,
}) async {
  throw UnsupportedError('Download local nao suportado nesta plataforma.');
}
